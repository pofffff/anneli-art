fragment cardGridBlock on CardGridRecord {
  id
  showDescription
  pagination
  background {
    hex
  }
  spacing
  centered
  shadow
  links {
    ... on PageRecord {
      id
      title
      description(markdown: true)
      pageSlug
      image {
        ...image1
      }
      # content {
      #   ... on HeroRecord {
      #     id
      #     image {
      #       ...image1
      #     }
      #   }
      # }
    }

    ... on LinkRecord {
      id
      image {
        ...image1
      }
      url
      title
    }
  }
}
